# Copy to ".env" in this directory (".env" is gitignored). Used by colab.ipynb.
# GitHub: classic PAT with "repo" read, or fine-grained "Contents: Read" on this repository.
GITHUB_TOKEN=

# Optional: Hugging Face token for gated models (e.g. Llama). If set, the notebook can skip interactive login.
# HF_TOKEN=
# (alias also supported: HUGGING_FACE_HUB_TOKEN)
